// DEMO 占位文件 —— 真实环境这里是 frameworks/native 仓的源码树（独立 git 仓）。
//
// 用途：演示"编辑本仓任意文件时，Claude Code 会按需加载同目录下的 CLAUDE.md"。
// 该 CLAUDE.md 由 SessionStart hook 按 features/<分支>/repos.tsv 物化，
// 内容 = features/dev-sidebar/frameworks-native.md；真实环境用本仓 .git/info/exclude 隔离。
