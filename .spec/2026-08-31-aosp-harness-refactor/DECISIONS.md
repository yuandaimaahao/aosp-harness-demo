# 2026-08-31-aosp-harness-refactor 项目口径

> 前面的 spec 确认过的口径记在这里，后续 spec 起草 requirements 前先读。
> 已被本文件覆盖的条目不许再标 [推断] 去问用户。

| 日期 | 来自 spec | 口径 |
|---|---|---|
| 2026-08-31 | — | 调研范围已确认：按 STATE.md 中所列“查/不查”边界执行工程审查与后续重构。 |
| 2026-08-31 | PLAN review 熔断 I1 | 三轮 review 后裁定：所有真实 device/CVD 调用必须显式传同一安全 `android-instance-id`，serial/CVD name 不得作 lease key。若判断错误，代价是需要额外配置 instance ID；若不做，代价是同一 CVD 可被 device/cvd 两种别名并发操作。 |
| 2026-08-31 | PLAN review 熔断 I2 | 三轮 review 后裁定：`harness_verify` 显式接收 `--session-id`、`--lease-wait-seconds`、`--android-instance-id`；wait 默认 0，只接受 CLI 覆盖，真实 v2 模式缺 session/instance ID 就返回 2。若判断错误，代价是 CLI 更繁琐；若不做，代价是实现者自行猜环境变量/默认值，导致租约无法可重现地获取。 |
| 2026-08-31 | PLAN review 熔断 M1 | 删除回滚矩阵中 `06` 消费 `05` 的错误引用。该项不承重；若仍有隐性依赖，代价是 `06` 将缺少明确契约，但当前三份直接边集均证明不存在该边。 |
| 2026-08-31 | 门① | 用户通过 PLAN v4 及默认 canonical-core 口径，并要求后续所有 spec 按 autopilot 模式执行。 |
| 2026-08-31 | 01-device-safety | 选择 `requirements-first` + `autopilot`：两个当前/预期安全行为及验收已明确，无需在 requirements 前做架构选型；autopilot 由用户显式指定。 |
| 2026-08-31 | 01 requirements review 熔断 I4 | R4 的验收矩阵固定为 Claude、Codex 每个入口各覆盖 serial 缺失与非空非法 `-bad`，共四个组合，并统一断言 flag 错误优先于 serial 校验。若判断错误，代价仅是增加两组离线 fixture；若不做，两个独立实现之一仍可能在非法 serial 下错误地先报 `ANDROID_SERIAL`。 |
| 2026-08-31 | 01 tasks review 熔断 I1-I3 | 测试任务最终拆成 fixture、非法/合法 serial、flag+Demo、skill block 提取、contract、mutation、legacy 聚合八片，生产改动拆成 Claude、Codex、skills 三片；代码步骤内嵌最小 Bash 骨架并显式串联全部消费。若判断错误，代价是 tasks 更长、调度步骤更多；若不做，复杂 oracle 仍可能空跑且独立执行者无法在快速 review 窗口内复现设计。 |
| 2026-09-01 | 01-device-safety 验收 | Claude 真实 verifier 的 serial 契约固定为显式 `ANDROID_SERIAL` 且匹配 `^[A-Za-z0-9][A-Za-z0-9._:-]*$`；缺失/非法值在零 ADB 后返回 `2`。Claude/Codex 的 `--allow-skip` 仅允许 Demo，真实模式的 flag 错误先于 serial 校验。两个部署 skill 的真机块采用唯一 preflight、固定 `adb -s "$device_serial"` 和保守单行 allowlist。后续公共 runtime/adapter 必须保持这些前置契约。证据：`work/2026-08-31-01-device-safety/acceptance-report.md`。 |
| 2026-09-01 | 02-offline-quality-gate 选片 | 选择 `requirements-first` + `autopilot`：PLAN 已确定 offline/CI 分流、自动测试发现和固定工具策略，不需要先做架构原型；autopilot 沿用用户对后续规格的显式授权。 |
| 2026-09-01 | 02 requirements 默认口径 | Offline 必需依赖包含现有回归实际使用的 Bash、Git、Python 3、ripgrep 与基础文本/coreutils 命令；缺失在检查前返回 `2`。CI 固定 ShellCheck 0.11.0、shfmt 3.14.0、Gitleaks 8.30.1，固定 `ubuntu-24.04` x86_64 官方资产并校验 requirements 所列 SHA-256；工具实际执行后的非零统一为检查失败 `1`。 |
| 2026-09-01 | 02 requirements baseline 裁定 | 受管 Shell 集合固定为工作树内排除 `.git/.spec` 的普通 `.sh` 或 Bash-shebang 文件，C locale 排序且不跟随软链。历史静态债务以锚点 `b143821925e279401334d09a788ba9a969df5c7c` 的批准 `path + git-blob` 集合豁免，baseline SHA-256 同时钉在 gate/test；新/变化内容不得扩增豁免。若判断错误，代价是历史债务延后清理；若不做，02 的 400 行预算无法形成绿色 CI。 |
| 2026-09-01 | 02 requirements contract 裁定 | Gate CLI 只接受唯一 `--offline`/`--ci`；依赖与 CI 工具预检必须早于 syntax/test。ShellCheck 参数固定为 `-x --severity=warning`，shfmt 固定为 `-d -i 2 -ci -bn`；`.gitleaks.toml` 精确继承默认规则且摘要为 `27630a...d76e`，CI 每次用运行时拼接的 AWS canary 证明真实规则有效，再扫描工作树。COVERAGE 固定五列和 active 状态。若判断错误，代价是 gate 协议更严格、fixture 更多；若不固定，空配置或参数漂移可制造假绿。 |
| 2026-09-01 | 02 requirements baseline 集合 | Canonical baseline 是锚点提交的全部 30 个受管入口，C-locale `path<TAB>blob` 文件摘要为 `62211b0b05c8ada6e48e408696244a655e1b0cb728c3a5406b601fc0af074a5f`；运行时不读取历史提交，以便 shallow CI 一致执行。 |
| 2026-09-01 | 02-offline-quality-gate 验收 | 根质量入口固定为 `scripts/check.sh --offline|--ci`：offline 只做 core 依赖、全受管 Shell 语法与自动发现的根测试；CI 再做固定版本工具预检、exact path+blob 增量静态检查、固定 config 的仓库外 Gitleaks canary 与工作树秘密扫描。成功末行精确为 `RESULT PASS  aosp-harness offline quality gate`，协议/环境错误 `2`，检查失败 `1`。workflow 固定 `ubuntu-24.04` 与三份官方摘要资产；coverage 只维护五列 active 映射，不声明数字覆盖率。证据：`work/2026-09-01-02-offline-quality-gate/acceptance-report.md`。 |
| 2026-09-01 | 03 requirements review round 1 | 补全 `03 -> 08` 为五个稳定 API：validate/path/write/read/remove；成功返回 `0`，普通 OS 操作失败 `1`，协议/安全错误 `2`，缺失/内容冲突 `3`。快照采用 create-once：同值幂等，异值不覆盖基线。若判断错误，代价是公共面多三个小函数；若不补，08 必须复制安全实现或依赖未声明私有函数。 |
| 2026-09-01 | 03 requirements review round 1-3 | Claude hook 按官方事件能力分级：UserPromptSubmit 退出 `2` 阻止 prompt；SessionStart 错误只保证不创建/注入状态并报错，不宣称阻止会话；SessionEnd 只保证 hook 被执行且未超时时的幂等清理。五种 SessionStart source 永不覆盖已有基线；startup/fork/clear/resume 可在缺失时建立活跃生命周期基线，compact 缺失报错。SessionEnd 后 resume 建新基线，不宣称跨结束检测漂移；项目 ID 为权威物理根完整 SHA-256，最低 Claude Code 版本 2.1.234。同步 command hook 的同 ID End 必须先于后续 resume Start 完成，并发跨客户端复用同一 session ID 不在保证内。 |
| 2026-09-01 | 03 requirements review round 2 | 五 API 的错误表固定为：成功 `0`，普通 OS 操作失败 `1` + `error: session state operation failed`，协议/安全错 `2` + `error: unsafe session state`；仅 write 异值冲突、read 缺失返回 `3`，remove 缺失幂等 `0`。信号只清本调用未提交临时文件，不删除既存或并发胜者快照；SessionEnd 经事件名/session ID/reason 校验后删除目标已提交快照。 |
| 2026-09-01 | 03 requirements review round 3 | 正常 publish race 从 absent 开始，loser 重新以 fd 验证安全胜者后按同值 `0`/异值 `3`；恶意 TOCTOU 专指既有安全快照 stat 后、`O_NOFOLLOW` open 前被换成不安全对象并返回 `2`。remove 同时自底向上尝试删除安全空 session/project/root 目录，遇到并发非空目录仍成功且不删其他条目，使 08 无需复制清理逻辑。 |
| 2026-09-01 | 03 design review round 1-2 / PLAN v5 | 两轮独立 reviewer 均判定原 03 的 358 行设计预算没有证明可实施，继续压缩会牺牲 raw-byte 校验、fd/信号竞态或测试 oracle。沿五个稳定 API 拆为 provider 安全内核 03 与 Claude 生命周期接入 03b，串行共享 coverage；若判断错误，代价是多一个 spec 和一轮接口 fixture，若不拆则更可能越过 400 行硬门或形成假绿测试。用户已要求后续按 autopilot 执行，增量 PLAN 门由该授权自动裁定。 |
| 2026-09-01 | 03 收窄 requirements review round 3 I1-I3 | write 的 HUP/INT/TERM 采用 shell 惯例 `128+signal`，分别返回 `129/130/143`，并写入 PLAN v5.1/API 表；若判断错误，调用者可能只需要“任意非零”，代价是契约略严，若不固定则清理后 rc 0 可假成功。受管环境路径的危险谓词固定为原始路径含控制字节或 `.`/`..` 组件，HARNESS 根额外拒绝 `/`；非空相对/父级不可规范化同样 rc 2。remove 在安全目录存在但 feature 缺失时仍 prune 空层级。 |
| 2026-09-01 | 03 收窄 design review round 1 B1 | Linux `rmdir(name, dir_fd)` 无法把最终删除原子绑定到已持有 directory inode；威胁保证收窄为确定性 marker 在最终 dev/inode 身份检查前替换时 fail closed。同 EUID 攻击者在检查后、rmdir 前换入空目录的窗口不承诺保护；`rmdir` 仍不会删除非空目录/内容。若判断错误，代价是同用户恶意进程可使一个空替换目录被移除；若继续宣称任意时刻安全，则接口在 Linux 现有 primitive 下不可实现。 |
| 2026-09-01 | 03 execute sizing backflow / PLAN v5.2 | task 1.1/1.2 的真实 diff 与独立 review 证明 validate+fresh path 已 373 行，原 359/400 整片 sizing 失真；继续拆 path 会合入半安全 public API。保持五 API 与 3 文件边界，03 单独改为 1200 目标/1400 硬门，并强制 16 个小任务逐个独立 diff review；其他 spec 仍为 400。若判断错误，代价是最终聚合 review package 变大，但没有单轮未审增量或半安全跨片接口。 |
| 2026-09-01 | PLAN v5.3 P5/P2 回流 | 独立 reviewer 判定1400行例外不能证明整片`<1h>`，且共享provider/test/coverage、后序自动测试没有absent分支会破坏独立回滚并暴露partial provider。按安全边界拆为03 foundation、03a path、03b snapshot、03c signals、03d remove+aggregator、03e Claude；每片独占不同模块/test/coverage fragment，每条私有边固定签名与present/absent inert契约。03d仅在五模块及预期私有函数完整时设置marker并发布五API，03e/08对每个missing-module状态走legacy；每片恢复400行门并维护六列首尾绑定review manifest。若判断错误，代价是多层私有模块和fixture CLI；若不做，代价是P5/P2失效且前序片无法独立回滚。 |
| 2026-09-01 | PLAN v5.3.1 依赖契约澄清 | 03a design review round 1 指出 path core 必须实际调用 foundation public validate，而原边说明只概括两 private export。将已存在的 `harness_validate_feature_name` 补入同一 `03 -> 03a` capability guard；不新增拓扑、owner或协议。若判断错误，代价仅是guard更严格；若不做，缺失validate时可形成partial export且fail-fast测试无法证明真实依赖。 |
| 2026-09-01 | PLAN v5.4 path race assurance 拆片 | 03a design round 2 证实311行原型仅执行fresh，不能证明全部三层mutation能进入400行。03a交付完整private provider与代表性root race，03a1只新增穷举race test；03b在两者PASS后才开始。若判断错误，代价是多一个自动测试片；若不拆，代价是再次执行期超门或删除承重安全oracle。 |
| 2026-09-01 | PLAN v5.5 anchor-driven race归03a1 | 03a task4 fix1实测未格式化实现397行，但按仓库pinned shfmt格式化为527行；只把project/session race放03a1仍不足。03a保留完整private provider、source/root/static、非anchor确定性错误分类probe与marker位置结构，全部root/project/session anchor-driven provider-copy mutation由03a1在任何consumer前验收。若判断错误，代价是03a单独不动态反证anchor；若不做，代价是400行/P5与CI格式门不可同时成立，或靠不可读压缩维持假预算。 |
| 2026-09-01 | 03-session-state-safety foundation 验收 | Foundation 只公开 `harness_validate_feature_name`；`_harness_session_state_foundation_path` 与 `_harness_session_state_run path` 保持私有，只保证 fresh root/project/session 的四级根选择、physical parent 与 fd 链。四个状态 public API 和 `HARNESS_SESSION_STATE_PROVIDER_VERSION` 必须持续不存在，直到 03d 完整 aggregator 发布。BASE `5038c545` 到 HEAD `ac985df3` 恰好两个文件、400 行，三任务 review manifest 连续且最终 PASS。证据：`work/2026-09-01-03-session-state-safety/acceptance-report.md`。 |
| 2026-09-01 | 03a-session-path-safety 验收 | Path片只交付private `_harness_session_path_core`与三个03a1测试anchor，不发布状态public API/marker；完整source/inert/root/static/结构回归在full与depth-1 checkout均PASS。BASE `d68911b` 到accepted HEAD `f91f54d3` exact两文件391行，四任务manifest连续且固定ShellCheck/shfmt全PASS；实现以merge `2f07821`合入main。全部anchor-driven race仍由03a1在任何consumer前验收。证据：`work/2026-09-01-03a-session-path-safety/acceptance-report.md`。 |
| 2026-09-02 | PLAN v5.6 race assurance拆片 | 03a1 round6完整单文件原型37/37与14项自反证均PASS，但固定格式411/400，不可删oracle强行压缩。round7将private driver/self-test与root matrix拆成400/400、109/400，补齐workspace/log物理隔离后full/depth-1及损坏态实跑PASS。因此保留03a1拥有`tests/lib/session-path-race-driver.py`，新增03a2拥有`tests/test-session-path-races.sh`；driver物理缺席时entrypoint inert以支持独立回滚，存在但类型/protocol/执行损坏必须fail closed。03a2的inert PASS不能解除03b门，必须有dependency-present的37-case与九类计数证据。若判断错误，代价是多一个私有测试协议与spec；若不拆，则P5/400行门再次失效或丢失承重安全证据。 |
| 2026-09-02 | 03a1-session-path-race-assurance 验收 | Private driver只发布`protocol/self-test/run-matrix`测试CLI，不进入root默认发现、不发布运行时API。accepted HEAD `1c6e14f0`相对BASE `c959efaf` exact新增一个400行driver；37/37、九family、14项真实自反证、path/log capability、full 118 commits与真实depth-1 1 commit均PASS，五任务manifest连续全PASS；实现以merge `bf489b0`合入main。03a2只能在该记录之后创建base/worktree/dispatch，并必须提供dependency-present的默认37-case证据。证据：`work/2026-09-01-03a1-session-path-race-assurance/acceptance-report.md`。 |
| 2026-09-02 | 03a2-session-path-race-matrix 验收 | Root默认发现入口独占37-row四列matrix、provider/driver/core优先级、inert surface与case-log复核；入口每次精确调用driver `protocol`与`run-matrix`各一次、绝不调`self-test`。accepted HEAD `b9582e51`相对BASE `6f26119e` exact新增一个141行可执行脚本；dependency-present 37/37、九类`9/3/9/3/3/3/3/3/1`、三任务manifest、full/depth-1/rollback、fixed tools、offline及collection后终门全PASS；实现以merge `744acdc`合入main。只有这些dependency-present证据入ledger后才可启动03b，inert PASS不能替代。证据：`work/2026-09-02-03a2-session-path-race-matrix/acceptance/acceptance-report.md`。 |
| 2026-09-02 | PLAN v5.7 snapshot signal seam/assurance拆片 | 03b requirements round1证明03c无法在不修改03b或复制算法时清理封装在03b Python worker中的owned temp；child handler、temp与publish点因此归03b。PID复审增加可被03c直接background并最终exec Python的spawn-only worker；design复审再要求child先锁存首信号、close前转移ownership、cleanup close全遍历且不遮蔽信号码，并明确rename线性化。普通core只在隔离subshell调用worker。新增03b1作为完整assurance与03c顺序门。design round2修复后03b原型`a708ce6`固定格式208+192=400/400，完整source/private/public/inventory/export、strict worker、held capture、静态攻击整树delta、字节级read、并发winner及四类信号窗口全绿；03b1原型`cbdbdde`固定格式308/400、240项调用/状态delta，补齐failure双流、换入对象身份/shape、EEXIST全oracle前清temp/完整winner指纹、post-close/early-second/post-rename-success/signal+cleanup-close-error，并证明owned temp、session与三层ancestor共五次close全部尝试，仍有92行整合余量。若判断错误，代价是多一个私有export和自动测试片；若不改，03c会杀错PID、二次打断cleanup或让安全测试假绿。 |
| 2026-09-02 | 03b-session-snapshot-safety 验收 | Snapshot 片只交付 spawn-only `_harness_session_snapshot_worker write\|read` 与两个 signal-aware 私有 core，不发布状态 public API/marker；child 首信号原子锁存、三信号转 ignore、close 前转移 ownership、cleanup 全 fd 遍历且 latched 129/130/143 优先于 cleanup 错误；发布只用 `renameat2(RENAME_NOREPLACE)`，ENOSYS/缺 symbol 不降级。accepted HEAD `ab1e870e` 相对 BASE `3d15a0d7` exact 两文件 208+192=400 行，八任务 manifest 连续全 PASS，candidate/full/depth-1/rollback/order 五路验证与 post-merge 回归全绿；实现以 fast-forward `ab1e870` 合入 main。03b1 只能在 accepted HEAD、active 证据、八 anchor、signal 协议、exact2/400 与全 PASS manifest 入 ledger 后启动（本行即为该记录）；03c 继续全部缺席。证据：`work/2026-09-02-03b-session-snapshot-safety/acceptance/acceptance-report.md`。 |
| 2026-09-02 | 03b1 requirements 上游集合裁定 | 自 03b1 起，「上游 tracked 文件」统一为六个：foundation、path、03a1 driver、03a2 entrypoint、03b snapshot runtime、03b 基础测试；SHA 校验集与 BASE..HEAD 零变更 diff 集必须同集，后序片（03c/03d/03e）在此基础上累加各自前序交付。若判断错误，代价是某片的上游保护漏一个文件；若不做，同类口径每片各写一份会再次漂移。 |
| 2026-09-03 | 03b1-session-snapshot-assurance 验收 | Assurance 片只交付 exact 单文件 `tests/test-session-snapshot-assurance.sh`（346≤400），只复制 provider 在八 anchor 注入攻击，不修改前序模块、不发布运行时 API/marker；断言 241=prototype 240+`ASSURANCE_UNLINK_LOG` rename 已提交窗口旧名 ENOENT oracle 1。accepted HEAD `c7a18edf` 相对 BASE `8a164f21` exact 单文件，七任务 manifest 连续全 PASS，candidate/full/depth-1/rollback/order 五路零 delta 与 post-merge 回归全绿；实现以 fast-forward `c7a18ed` 合入 main。执行期修订 tasks.md E3 export 检查为先 source 两个上游 lib 再 `declare -F`（健康 provider 的 export 以之为条件），经独立 review 确认最小且 fail-closed 语义不变。03c 启动门（dependency-present 完整矩阵 checks=241、exact1/400、full/depth-1/offline 与回滚证据入 ledger）由本行满足；inert PASS 未作为验收证据。证据：`work/2026-09-02-03b1-session-snapshot-assurance/acceptance/acceptance-report.md`。 |
| 2026-09-03 | 03c-session-write-interrupts 验收 | Signals 片只交付 source-inert 信号转发 facade（唯一私有 export `_harness_session_write_with_signals`，正 PID 单点转发、spawn-gap 补转发、first-signal-wins 锁存、恰 129/130/143 与 0/1/2/3 透传、双流空零外部命令）与默认发现信号矩阵测试（checks=139，三窗口×三信号+process-group 三行+双 mutant 自反证），不发布状态 public API/marker、不修改上游七文件。accepted HEAD `648fe667` 相对 BASE `c9c82264` exact 两文件 45+325=370≤400，八任务 manifest 连续全 PASS，candidate/full/depth-1/rollback/order 五路零 delta 与 post-merge 回归全绿；实现以 fast-forward `648fe66` 合入 main。执行期修订：任务 1.2 round1 F1（spawn-gap 行≈17% 假红，补转发落入 child pre-exec 窗口被吞）经测试侧修复闭合——gap 行 oracle 改前向观测日志、driver 放行、timeout 兜底、winner 断言 absent/beta 析取，design/tasks 六处「执行期修订」同步。03d 启动门（dependency-present active 证据、exact2/400、full/depth-1/rollback 与 03d 顺序门证据入 ledger）由本行满足；inert PASS 未作为验收证据。证据：`work/2026-09-03-03c-session-write-interrupts/acceptance/acceptance-report.md`。 |
| 2026-09-03 | 03d-session-remove-prune 验收 | Provider 片交付 remove 模块（唯一私有 export `_harness_session_remove_core`，non-creating verified remove、`PRUNE_BEFORE_IDENTITY` held fd identity 核对后自底向上 prune、0/1/2 固定 stderr、ENOENT/ENOTEMPTY 幂等）与 complete-provider aggregator（五模块 preflight→唯一顺序静默 source→9 export 点名→单临界区四转接+marker=1，任一失败静默 rc1 双流空）及默认发现集成测试（checks=72，remove 矩阵+发布面+六类 inert fixture+双 anchor 注入+双 mutant 自反证）与独占 coverage fragment，不修改上游九文件。accepted HEAD `388a83d5` 相对 BASE `d8c2baae` exact 四文件 110+46+18+204=378≤400，九任务 manifest 连续全 PASS，candidate/full/depth-1/rollback/order 五路零 delta 与 post-merge 回归全绿；实现以 fast-forward `388a83d` 合入 main。执行期修订：任务 1.2 round1 B1（语法错模块经 source 泄漏 stderr 违反 R6 双流空）五个 source 各加 `2>/dev/null` 并 amend 保持单提交，round2 PASS；无 design/tasks 机制修订。03e 启动门（dependency-present active 证据 checks=72、exact4/400、full/depth-1/rollback 与 03e 顺序门证据入 ledger）由本行满足；inert PASS 未作为验收证据。证据：`work/2026-09-03-03d-session-remove-prune/acceptance/acceptance-report.md`。 |
| 2026-09-03 | 03e design review round 1 | SessionEnd reason 合法值集钉死为官方五值 `clear`/`resume`/`logout`/`prompt_input_exit`/`other`（官方 SessionEnd reason 表，最低版本 2.1.234 起 `bypass_permissions_disabled` 已移除）；2026-09-01「round 1-3」「round 2」两行只确认「经事件名/session ID/reason 校验后删除」未枚举止值集，本行补齐。若漏收 `resume`，交互式 `/resume` 切换会话触发的正常 SessionEnd 会被判非法而零删除，造成已结束会话状态永久泄漏。 |
| 2026-09-04 | 03e-claude-session-lifecycle 验收 | Claude 三 hook 经 complete-provider guard 消费 v1 会话状态：SessionStart 按 source 分级建/读基线（compact 缺失报错不创建、五 source 永不覆盖）、UserPromptSubmit 漂移 exit 2、SessionEnd 校验后幂等 remove；compat marker 字面量 `compat: session-provider=legacy` 每 hook 结构恰 1 处。demo 全部持久写入限于 `mktemp -d "${TMPDIR:-/tmp}/claude-harness-demo.XXXXXX"` 与单 EXIT trap。accepted HEAD `5b2e66b3be9079aa31b4b6eba2da88888a3aaeba` 相对 BASE `cc04996e1e405c00e16be3b57d3ef62d90cd7fd1` exact 六文件 367≤400；dependency-present 摘要逐字节 `RESULT PASS  claude session lifecycle\n`，checks present=137 / absent=12；八任务 manifest 连续全 PASS；candidate/full/depth-1/rollback/顺序门与隔离 check-converge 全绿。R7「mktemp 外净变更=0」只在树根起始状态自洽时成立：`claude-feature --dry-run` 仍会改 tracked `CLAUDE.md`（`sync_feature_link` 在 dry-run 前，b2fab19 既有、03e 未引入）。后序片启动门（dependency-present active 证据、exact6/400、full/depth-1/rollback 与顺序门证据入 ledger）由本行满足；legacy-only PASS 未作为验收证据。证据：`work/2026-09-03-03e-claude-session-lifecycle/acceptance/acceptance-report.md`。 |
| 2026-09-04 | 04 requirements 租约协议裁定 | 逻辑 owner 固定为 `EUID+$$+/proc/$$/stat starttime`，保证 command substitution 后父 shell 可释放并防 PID 复用；任意不同 owner 对同一规范 `domain+canonical_id` 的同/异 mode 都完全独占，只有同 owner+session+request hash 完整重入幂等，其余同 owner 重叠 rc2。request 是 C-locale 排序的规范 TSV+SHA-256，workspace `realpath` 结果拒绝 ASCII 控制字节；整 bundle 以单一 publish/unpublish 点全有全无，状态根为 EUID 自有 0700 真目录，I/O/状态损坏 fail-closed rc2，占用/超时 rc3，成功 rc0。门③必须用 runnable fixed-shfmt 三文件 prototype 证明 exact3/400，不成立则回流 PLAN 拆片。若判断错误，代价是 Linux `/proc` 约束与更严格路径集；若不固定，代价是 command substitution 后租约立即 stale、同 mode 并发穿透或组合租约偏有。 |
| 2026-09-04 | PLAN v5.8 lease assurance拆片 | 04 design round1真实证明原387/400 sizing存在CLI假绿且缺strict record/global overlap、工具/输出I/O收敛、Python3.8兼容和tombstone恢复，完整R9不可在余13行内可信实现。稳定public API不变：04保留全部runtime正确性、docs与基础合同测试，新增04a exact单文件穷举mutation/I/O/concurrency/adapter且不修改04三文件；修复与复审准备后的runnable fixed-shfmt原型为04 exact3=336+7+57=400/400、04a exact1=398/400，core固定内部结果协议，04a只替换唯一test seam并含完整字段/失败族与四类mutant自反证，两者全绿。05/06/08必须等待04a dependency-present证据，inert PASS不能解除；NEXT顺序改为04→04a→05。若判断错误，代价是多一片默认测试与顺序门；若不拆，代价是再次执行期超门或删除承重oracle形成假绿。 |
| 2026-09-04 | 04-runtime-resource-leases 验收 | `resource-leases-v1` 发布两个 Bash API：规范 TSV/request SHA-256、EUID+PID+starttime owner、跨 owner 键级完全独占、同 owner 完整重入、bundle 单点 publish/unpublish、monotonic deadline、stale/tombstone 恢复与固定 `0|2|3` 双流协议。accepted HEAD `f7cfcb202d1fd2934d07cc90e333a4205b563243` 相对 BASE `692d52d00b56df9609760aa33a6f9aa3c38095a3` exact 三文件 `336+7+57=400/400`；七任务 manifest 连续全 PASS，candidate/full/depth-1/rollback、fixed tools、四不变量、隔离 check-converge 与后序五类缺席门全绿。执行期发现并修复 deadline 前阻塞/过期后发布、两处测试 errexit 假绿与相对 TMPDIR；最终挂账 finding 为 0。只有本行 dependency-present active 证据、exact3/400 与全 PASS manifest 入库后才可启动 04a，04a 缺席/inert PASS 不得启动 05/06/08。证据：`work/2026-09-04-04-runtime-resource-leases/acceptance/acceptance-report.md`。 |
| 2026-09-04 | PLAN v5.9 deadline assurance相邻修复 | 04a requirements前实跑转交的398行assurance，在已合入04 provider上稳定失败且monotonic场景只有一次`flock`：扫描后首次观测deadline到达便直接rc3，未进入最后一次无sleep锁尝试。隔离provider只替换相邻两行，使到期路径立即continue并在下一轮拿锁后、读record前由既有检查返回3；requirements round1纠正全局churn口径并补出负向oracle缺口，修订assurance为fixed-shfmt 396行，default/all/dependency-absent全PASS，exact churn `2+2+396=400`。04a改为exact2/400边界，不改API/文档/基础测试；若判断错误，代价是多改已验收provider两行，若不修则04a active oracle无法成立且后续consumer建立在已知deadline缺口上。证据：`specs/2026-09-04-04a-runtime-resource-lease-assurance/reviews/requirements-prototype-boundary.md`。 |
| 2026-09-04 | 04a requirements active-route与mutant裁定 | 默认assurance必须从顶层`tests/`解析repo，并在进入矩阵前核provider恰为repo内普通非symlink文件；absent surface显式清除override，禁止以路径错误制造inert假绿。所有承重fixture的创建/改写/清理均显式失败收口；overlap、真实unpublish状态转换与bundle规范化production mutant必须先核唯一anchor、预期替换和source健康，再逐字命中专属失败标签，adapter只作为provider未变的key-selection fixture mutant。fixed-shfmt原型仍为396行，default/all真实active约30秒与dependency-absent均PASS，exact churn仍`2+2+396=400`。若判断错误，代价是测试入口更严格依赖仓库布局；若不固定，则摘要PASS无法区分完整active矩阵与零case inert，mutation自反证也可能因构造损坏假绿。证据：`specs/2026-09-04-04a-runtime-resource-lease-assurance/reviews/requirements-prototype-boundary.md`。 |
| 2026-09-04 | 04a requirements 三轮熔断生命周期裁定 | 达到`fix_loop_max=3`后，round3剩余顶层临时树与readiness两项均属承重并采纳：mktemp创建须核repo外/EUID自有0700/空普通目录；失败trap必须把cleanup失败改rc1，成功摘要必须晚于删除及物理缺席验证；holder/waiter/clock marker和adapter fixture改写失败立即专属收口。fixed-shfmt仍396行，不开第4轮改写review，只允许原reviewer只读融合验证最终artifact；若判断错误，代价是失败路径遗留临时树或并发fixture错因假绿，因此融合验证未PASS不得放门②。证据：`specs/2026-09-04-04a-runtime-resource-lease-assurance/reviews/requirements-04a-runtime-resource-lease-assurance-round-3.md`。 |
| 2026-09-04 | 04a-runtime-resource-lease-assurance 验收 | 04a以provider deadline相邻`2/2`修复闭合最后一次无sleep锁尝试，并新增fixed-shfmt 396行默认发现assurance；不改public API、状态格式、文档或基础测试。accepted HEAD `3f17cf66c1a13296d77ed1f900109fc1feb633c6`相对BASE `ffb05899c33d04b4c3d1c6605b3d39b1e6a05204` exact两文件总churn400；default/all active、absent/damaged、状态/I/O/并发/adapter/四mutant、candidate/full/depth-1/rollback、四行manifest、fixed tools、四不变量、隔离converge与05五类缺席门全绿；实现fast-forward合入main。Task4首轮synthetic dispatch rc证据被阻断，改为真实`rg /dev/null` observed rc1后同reviewer复审PASS，最终挂账0。只有本行dependency-present active证据、exact2/400与全PASS manifest入库后才可启动05/06/08；inert PASS未作为验收证据。证据：`work/2026-09-04-04a-runtime-resource-lease-assurance/acceptance/acceptance-report.md`。 |
| 2026-09-04 | 后续流程统一 autopilot | 用户明确要求本项目后续所有流程均走 autopilot 且不再提问。自05起每个新spec固定以`--mode autopilot`创建，所有需求选择、审查门禁、回流与复盘由agent按spec规则自行裁定并写入ledger；仍保留独立review、机械硬门和失败回流，不把“不再询问”解释为跳过验证。项目为个人项目，提交只需清楚可追溯，不套组织提交模板。 |
| 2026-09-04 | PLAN v6.0 verifier owner收窄 | 05 requirements round1 B1证实“05同测三入口”与既定文件owner冲突，round2 P-B1又证实修改既有common入口仍与09重叠且回滚不可判。05改为新增`common/.harness/bin/verify-sidebar.sh` physical provider并独占contract doc/test；09消费05/06/07后独占dispatcher、三旧入口薄化、legacy fallback及parity，永不修改05 provider，05回滚只移除exact3并由09按物理缺席切fallback。若判断错误，代价是三入口漂移要到09才闭合；若不改，代价是跨片反复改入口、05回滚覆盖09 hunk或在400行门下删承重oracle。证据：`specs/2026-09-04-05-verifier-contract/reviews/requirements-05-verifier-contract-round-{1,2}.md`。 |
