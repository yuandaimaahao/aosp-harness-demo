# PLAN v5.5 round 2 execution-backed sizing report

## Verdict

PASS — both slices have runnable, pinned-tool-clean prototypes below their immutable limits. The 03a exact-two-file projection is `392/400`; the standalone 03a1 race prototype is `269/400`. No implementation worktree file was modified.

## Primary artifacts

- `v5.5-round2-test-session-path.prototype.sh`: runnable 03a core-test candidate.
- `v5.5-round2-test-session-path-races.prototype.sh`: runnable 03a1 three-layer shared driver.
- `v5.5-round2-core-evidence.log`: exact core/default/inert/static-oracle evidence.
- `v5.5-round2-race-evidence.log`: exact race case inventory and stream evidence.

Both scripts default to implementation worktree HEAD `fad7bf384d9d8807f1f268649bc8ed19e10cf4b0`; another root can be selected only for prototype execution with `HARNESS_PROTOTYPE_ROOT`. That prototype seam replaces the production test's one-line repository-root calculation, so it does not undercount the projected production file.

## Pinned tool gate

The binaries were mechanically checked before use:

```text
ShellCheck - shell script analysis tool
version: 0.11.0
shfmt v3.14.0
```

Fixed argv, both rc `0`:

```bash
shellcheck -x --severity=warning \
  common/.harness/lib/session-state-path.sh \
  v5.5-round2-test-session-path.prototype.sh \
  v5.5-round2-test-session-path-races.prototype.sh
shfmt -d -i 2 -ci -bn \
  common/.harness/lib/session-state-path.sh \
  v5.5-round2-test-session-path.prototype.sh \
  v5.5-round2-test-session-path-races.prototype.sh
```

The executed binaries were `/tmp/aosp-gate-review.T8RDQ1/shellcheck/shellcheck` and `/tmp/aosp-gate-review.T8RDQ1/shfmt`; acceptance must likewise resolve fixed binaries and assert their versions rather than trust PATH names.

## 03a core candidate

| Owned content | Formatted lines |
|---|---:|
| provider at `fad7bf3` | 114 |
| core test prototype | 278 |
| exact2 projection | **392/400** |

The candidate keeps `source-validate`, `roots-static`, the deterministic non-anchor `sitecustomize.py` post-mkdir disappearance probe, 2×2 isolation, static root/project/session attacks, exact child stream propagation, and the three anchor occurrence checks. It contains no provider-copy/anchor replacement driver and does not accept `--case mutations`.

The structure oracle extracts the lexical `open_managed` Python body, then proves every one of `before_mkdir`, `after_eexist`, and `before_open` occurs exactly once both globally and inside that body. A self-disproof copy moved `before_mkdir` into `dispatch` while keeping its global count at one; the candidate rejected it with rc `1`, empty stdout, and exact stderr `FAIL phase before_mkdir global or managed-body count\n`.

Normal default, `source-validate`, `roots-static`, and explicit dependency-absent each returned rc `0`, exactly 33 stdout bytes (`RESULT PASS  session path safety\n`), and empty stderr. In a real fixture containing the provider but no foundation file, both default and `--dependency-absent` returned the same exact result, proving neither path is stopped by a global foundation require.

The result is credible but tight: only 8 lines remain. Task 4 must apply the measured shape without adding new core-test cases; any growth must be offset by shared-helper compression without deleting an oracle.

## 03a1 runnable shared driver

The standalone prototype is **269 lines**, leaving **131 lines**. It uses one provider copy/count/replace driver, one exact subprocess protocol oracle, and one filesystem signature/inventory implementation. Default execution ran 19 real child processes/cases and returned rc `0`, exactly 41 stdout bytes (`RESULT PASS  session path race assurance\n`), and empty stderr.

Executed matrix:

- 9 swaps: the Cartesian product `root/project/session × safe-dir/link/file`; every case validates unsafe/`2`, replacement type/identity, original directory and victim dev/inode/mode/UID/content signature, exact allowed inventory keys, injection hit, and no traversal beneath the replaced object.
- 3 EEXIST cases distributed across layers: safe root winner (`0`), unsafe project winner (`2`), disappearing session winner (`1`), including made/catch sentinels.
- One cross-layer representative each for mkdir-success replacement, wrong EUID, mkdir failure, post-mkdir disappearance, open disappearance, final-stat disappearance, and real EIO.
- Explicit dependency-absent plus real-provider-absent default/flag all returned the same 41-byte PASS summary with no dynamic case execution and no path/public capability surface.

## Remaining full 03a1 matrix

The prototype proves the shared-driver shape and all attack mechanisms but is not the final exhaustive 03a1 delivery. With a three-layer full matrix, 18 cases remain:

- wrong EUID: root and session (`2`);
- EEXIST safe/unsafe/disappearing: the six missing layer/category pairs (`6`);
- mkdir-success replacement: project and session (`2`);
- mkdir failure: root and session (`2`);
- post-mkdir disappearance: root and project (`2`);
- open disappearance: project and session (`2`);
- final-stat disappearance: root and project (`2`).

Real EIO is layer-independent and already executed. The completed target is therefore 37 dynamic cases. The missing cases must be added as data rows/calls over the existing shared helpers, not copied bodies. A 131-line margin is credible for the remaining matrix, stronger full-inventory assertions for non-swap cases, self-disproofs, and the final dispatcher; if implementation duplicates per-layer bodies or exceeds 400, it must return to PLAN.

## Reproduction

From the main repository:

```bash
bash .spec/2026-08-31-aosp-harness-refactor/work/2026-09-01-03a-session-path-safety/v5.5-round2-test-session-path.prototype.sh
case_log=$(mktemp)
HARNESS_PROTOTYPE_CASE_LOG="$case_log" \
  bash .spec/2026-08-31-aosp-harness-refactor/work/2026-09-01-03a-session-path-safety/v5.5-round2-test-session-path-races.prototype.sh
rm -f "$case_log"
```

All prototype-owned fixture state is under `mktemp` and removed by traps；the caller-owned optional case log is explicitly removed by the reproduction commands above. The implementation worktree remained at `fad7bf3` and clean before and after every run.
