# review 包 fb161f9f..d1144174

## commit 列表

```
d114417 fix(codex): reject allow-skip outside demo mode
```

## diff --stat

```
 codex/features/dev-sidebar/verify-sidebar.sh | 5 +++++
 1 file changed, 5 insertions(+)
```

## diff

```diff
diff --git a/codex/features/dev-sidebar/verify-sidebar.sh b/codex/features/dev-sidebar/verify-sidebar.sh
index 4d117fe..a39e421 100755
--- a/codex/features/dev-sidebar/verify-sidebar.sh
+++ b/codex/features/dev-sidebar/verify-sidebar.sh
@@ -37,20 +37,25 @@ while (($# > 0)); do
     --help)
       usage
       exit 0
       ;;
     *)
       usage_error
       ;;
   esac
 done
 
+if ((demo == 0 && allow_skip == 1)); then
+  echo 'error: --allow-skip requires --demo' >&2
+  exit 2
+fi
+
 if ((demo == 0)); then
   serial="${ANDROID_SERIAL-}"
   if [[ -z "$serial" || ! "$serial" =~ ^[A-Za-z0-9][A-Za-z0-9._:-]*$ ]]; then
     echo 'error: set ANDROID_SERIAL to a safe, explicit target serial' >&2
     exit 2
   fi
   ADB=(adb -s "$serial")
 fi
 
 pass_count=0
```
