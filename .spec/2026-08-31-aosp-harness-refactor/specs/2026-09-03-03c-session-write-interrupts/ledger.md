# ledger — spec: 2026-09-03-03c-session-write-interrupts
# plan: （PLAN.md 路径与版本；小需求写「无」）
# worktree: （工作区路径）

> 会话压缩之后，「我刚做完什么」的记忆不可靠。这份文件和 git log 才是。
> `任务 N: 完成` 是唯一的恢复锚点。

---
- requirements round1：PASS（阻断0/重要0/次要2），报告：reviews/requirements-03c-session-write-interrupts-round-1.md。次要1（R4「child的process-group」在非job-control shell下如何建立/转发而不打回facade自身）留门③ design正面回答；次要2（R6「flag」缩写指代--dependency-absent）措辞紧凑、不改。起草期一次ID修正：NEXT按PLAN第53行定为03d下一切片（规范ID全名见PLAN，按tasks裁定6本片ledger不逐字记录）并改为日期无关机械形式（控制器指示有误、起草者按指示落地后由控制器复核PLAN发现，唤回修订）。check-req/check-criteria/check-analyze/check-plan与diff --check全rc=0。
- 自动通过: 门② requirements按autopilot通过。R1–R8全[计划]来源、frontmatter五字段+已由用户确认、主验证命令与验收清单/四不变量（含上游七文件同集）闭合；独立review一轮即PASS；无[推断]/[默认]待问项。如果错了代价：门②误放会让design建立在错口径上——reviewer逐字对照PLAN/DECISIONS/实际代码排除。
- design round1：PASS（阻断0/重要0/次要4），报告：reviews/design-03c-session-write-interrupts-round-1.md。机制正确性经reviewer实际代码逐行核对+mktemp内7组bash语义实测：正PID单点转发覆盖义务（worker稳态无孙进程）、trap先装+pending锁存补转发无窗、first-signal-wins双侧幂等、wait循环rc>128&&kill -0无死锁、不打回facade自身、spawn-gap mutant经barrier封闭无假绿路径；setsid 2.39.3/set -m泄漏两条实测声明独立复跑证实；门②次要1已正面回答。四条次要：trap摘除不恢复调用方trap（留03d留意）、sizing纯分解预算无runnable prototype（numstat≤400执行期兜底）、「无孙进程」系稳态事实（措辞备注）、mermaid未经渲染器（离线无mermaid-cli，人工核对语法）。如果错了代价：机制误判会让03c执行期返工甚至假绿——reviewer已实测排除。
- 自动通过: 门③ design按autopilot通过。九节齐全、R映射8/8、frontmatter消费/产出逐字一致、mermaid 3图人工核对合法、sizing 45+345=390≤400闭合、文件清单与验收资产闭合；check-req/check-criteria/check-analyze对requirements仍rc=0、diff --check rc=0；独立review一轮即PASS。
- tasks round1：NEEDS_CHANGES（阻断1/重要1/次要3），报告：reviews/tasks-03c-session-write-interrupts-round-1.md。B1=任务1.2步骤7累计断言误用$TASK_BASE..$TASK_HEAD（该范围只含本任务单提交、恰两文件断言必失败）；I1=裁定6禁令范围与任务2.5报告commands字段冲突；M1=顺序门ls依赖无nullglob前提未写明；M2=`! ls && test`在set -e下短路陷阱；M3=mktemp变量衔接不明。唤回原起草者修复后交全新reviewer。
- tasks round2：PASS（阻断0/重要0/次要0），报告：reviews/tasks-03c-session-write-interrupts-round-2.md。B1改$BASE_SHA口径并内联理由、I1裁定6收窄到scoped rg三类文件+报告/日志用"$NEXT"间接形式、M1前提写明、M2拆独立命令、M3统一$tmp，逐条闭合；全量重读无新增。范围外观察2条备查（R8 nullglob措辞、$tmp既有风格）。门④落盘期控制器自查发现本ledger第9行（门②时写入、早于裁定6）逐字含03d规范ID全名，属任务2.5 scoped rg域内自命中隐患，已改为间接形式并复核全域（九份ledger+五份execution-base.env）仅此一处。如果错了代价：B1不修会让任务1.2提交门必然假红、裁定6域不收窄会让实现者在证据义务与禁令间二选一踩坑、ledger字面残留会让任务2.5/2.6顺序门自命中假红——均已排除。
- 自动通过: 门④ tasks按autopilot通过。八任务（1.1交付≤45行、1.2测试≤345行、2.1–2.6零delta）与03b/03b1同构；R1–R8映射完整、exact两文件+sizing 390≤400、探针恰2处、mutant双设计、顺序门日期无关、裁定6/7防自命中与inert滥用；check-tasks rc=0；round1五findings全修、round2全新reviewer PASS。如果错了代价：tasks口径错会让执行期八任务连环返工——两轮独立review+控制器域内自查排除。
- 执行隔离：execution BASE=c9c82264b3f819a6a6449a0242e102e97b35c3b0（门④提交），分支 spec/2026-09-03-03c-session-write-interrupts，worktree=work/2026-09-03-03c-session-write-interrupts/worktree（clean 已核），execution-base.env 已写；固定工具门复验 shfmt v3.14.0 / shellcheck 0.11.0 在场。如果错了代价：BASE 错会让 exact2/400 与上游七文件不变量全部失真——逐字核对 git rev-parse 输出。
- 任务 1.1: 完成 — commits=[a8d3859f639eed64b0d1e74eb1461c8e32e64a45] reviewer=kimi PASS(0/0/0，报告 task-1.1-review-round-1.md)。单文件 45/45 行、双 marker exact-once、静态门/source 契约/语义抽查（143/130/first-wins/re-wait/inert）全独立复跑通过；实现者两处工具用法偏差（git add -N 后需 git add、review-package.sh 4 参签名）经 reviewer 判不影响交付正确性。如果错了代价：facade 状态机谎言会让 1.2 矩阵建在沙上——reviewer 已用假 worker 实测基本转发/锁存/重 wait。
- 任务 1.2 review round1：NEEDS_CHANGES（阻断1/重要0/次要0，报告 task-1.2-review-round-1.md）。F1=spawn-gap 三行对正确模块间歇 hang（reviewer 18 跑 3 败≈17%，strace 实证补转发落入 child pre-exec 窗口：SIGINT 被异步 subshell SIG_IGN 吞、SIGHUP 落入继承 trap no-op 窗口），失败签名与 mutant-a 不可区分→假红。controller 裁定：模块侧修复是死路（可靠送达 pre-exec child 需就绪握手，违反模块零外部命令/零文件读写约束；重试转发有 PID 复用风险；生产语义「facade 必转发一次、child 接收尽力而为」本身正确，R4 义务是转发不是保证送达）→ 走测试侧修复：gap 行 oracle 改为确定性的「补转发已执行」观测（probe 注入日志或 kill 遮蔽，实现者择机制），driver 释放 barrier 使存活 child 正常完成、消除 hang 依赖，mutant-a 以「无前向观测日志」确定性判 FAIL，timeout 仅作兜底；design 测试策略节的 gap 机制段由 controller 同步修订（03b1 E3 先例）。如果错了代价：不修则 2.1/2.2/2.3 三个 checkout 每次 15-20% 假红、default 入口不可作验收面。
- 任务 1.2: 完成 — commits=[648fe667396e6273f7d497479f16d7daf4560d18] reviewer=kimi PASS(round2 0/0/1，报告 task-1.2-review-round-{1,2}.md)。round1 F1 阻断（gap 行≈17% 假红）经测试侧修复闭合：前向观测日志 oracle+driver 放行+absent/beta 析取，flake 实证 reviewer 自跑 20/20 全绿、mutant-a/b 确定性 rc1；design.md 四处+tasks.md 两处「执行期修订」同步（F2 次要闭合）；checks N=139、文件 325 行、BASE..HEAD 两文件 numstat 370≤400。如果错了代价：gap 行假红未闭合会让 2.1–2.3 三个 checkout 验收面不可用——round2 reviewer 独立 20 连跑+自构双 mutant 排除。
- 任务 2.1: 完成 — commits=[648fe667396e6273f7d497479f16d7daf4560d18] reviewer=kimi PASS(0/0/0，报告 task-2.1-review-round-1.md)。candidate 审计全绿：静态门只 exact 两文件、default 固定摘要逐字、offline 发现恰一次、上游七文件 SHA 不变、BASE..HEAD 恰两文件 numstat 370≤400、clean。ACCEPTED_HEAD 固定为 648fe667396e6273f7d497479f16d7daf4560d18。如果错了代价：accepted HEAD 固定错会让后续 full/depth-1/rollback 全验错对象——reviewer 独立核 HEAD 逐字等于任务 1.2 TASK_HEAD。
- 任务 2.2: 完成 — commits=[648fe667396e6273f7d497479f16d7daf4560d18] reviewer=kimi PASS(0/0/1，报告 task-2.2-review-round-1.md)。reviewer 自建 full clone 独立重跑：HEAD 逐字=ACCEPTED_HEAD、rev-list count=189 完整历史、default 固定摘要逐字、offline 发现恰一次、上游七文件 SHA 前后不变、clean。次要1（report 把 manifest 划归 controller 的措辞与 tasks 步骤 4 字面不符）按 03b1 既定实践处置：manifest 行一律 controller 在 PASS 后追加，不改文档。如果错了代价：full checkout 假绿会让 depth-1/rollback 验证失去完整历史对照——reviewer 独立 clone 实跑排除。
- 任务 2.3: 完成 — commits=[648fe667396e6273f7d497479f16d7daf4560d18] reviewer=kimi PASS(0/0/0，报告 task-2.3-review-round-1.md)。reviewer 自建真浅克隆独立重跑：file:// depth-1、HEAD 逐字=ACCEPTED_HEAD、rev-list count=1、.git/shallow 恰一行、default 固定摘要逐字、offline 发现恰一次、上游七文件 SHA 不变、clean。如果错了代价：depth-1 假绿会让「最小 checkout 可用」承诺失真——reviewer 独立 clone 实跑排除。
- 任务 2.4: 完成 — commits=[648fe667396e6273f7d497479f16d7daf4560d18] reviewer=kimi PASS(0/0/0，报告 task-2.4-review-round-1.md)。reviewer 独立重放 rollback：exact 只删本片两文件（两行 D）、03b/03b1 测试逐字 PASS、offline 本入口发现 0 次、rollback commit b6ecfdd 实证不落任何真实分支（两仓库 cat-file 均 fatal）。如果错了代价：rollback 不 exact 会让「可无损撤出」承诺失真——reviewer 独立 clone 重放排除。
- 任务 2.5: 完成 — commits=[648fe667396e6273f7d497479f16d7daf4560d18] reviewer=kimi PASS(0/0/0，报告 task-2.5-review-round-1.md)。03d 顺序门 reviewer 独立重跑全过：nullglob off、spec/work 目录缺席、分支/worktree 零匹配、scoped rg 15 文件零匹配；裁定 6 遵守核验：本片 ledger 对 03d 字面全名零命中、报告/日志仅 NEXT 定义行一处且命令全间接形式。如果错了代价：顺序门假绿会让 03d 提前开工破坏切片边界——reviewer 逐门独立实跑排除。
- 任务 2.6: 完成 — commits=[648fe667396e6273f7d497479f16d7daf4560d18] reviewer=kimi PASS(0/0/2次要观察，报告 task-2.6-review-round-1.md)。八行六列 manifest awk 全量核验通过（相邻连续、首行 base=execution BASE、末行 head=ACCEPTED_HEAD）。终交付锚点 session-signals-facade-v1；accepted HEAD=648fe667396e6273f7d497479f16d7daf4560d18；active 摘要：default 固定摘要逐字 RESULT PASS（双空格）session write interrupts、checks=139（default==all 逐字一致、inert 探针 checks=0）；双 anchor 注入：BEFORE_SPAWN/AFTER_WAIT probe 包装+F1 修复后 gap 行前向观测日志 oracle/driver 放行/absent-beta 析取；两 mutant 自反证：mutant-a reforward 观测缺失确定性 FAIL（failures=6 无 hang）、mutant-b 锁存覆盖 FAIL（failures=15）；exact2/400：恰两文件 numstat 370；四不变量：winner 指纹不变（bystander 承载）、owned temp=0、上游七文件 BASE..HEAD 零变更、双流空；inert PASS 不作本片验收证据亦不解除 03d 顺序门；full/depth-1/rollback/order 四路验证全绿（rev-list 完整历史/count=1 shallow/恰两行 D 03b+03b1 逐字 PASS/scoped rg 15 文件零匹配）。次要观察 O1/O2 已在本锚点补齐（absent-beta 析取与四不变量逐字复述）。如果错了代价：终任务假绿会让整片验收建立在缺环证据上——reviewer 逐条对照前七任务源报告抽验+运行门终跑排除。
- 验收：六块全过。(1) 主验证本消息内实跑：candidate default rc0、stdout 逐字 RESULT PASS（双空格）session write interrupts\n、stderr 0B；offline rc0、本入口发现恰 1 次、末行 PASS；四不变量——exact2（恰 signals 模块+测试两文件）、numstat 370≤400、上游七文件 BASE..HEAD 零变更（0 行）、check-converge rc0。(2) R1–R8 对照：R1/R2 source-inert 双态（1.1/1.2 fixture 矩阵）、R3 组合不修改（snapshot 模块 SHA 前后不变）、R4 转发义务（facade/gap/group 12 行+前向观测）、R5 rc 映射（恰 129/130/143 与 0/1/2/3 透传）、R6 默认发现（default==all 逐字、offline 恰一次）、R7 三 checkout（candidate/full/depth-1）、R8 rollback exact+03d 顺序门。(3) 裁定重报：裁定 1 一次性交付（两任务各单提交）、裁定 2 探针字面量恰 2 处、裁定 3 双 mutant 自反证（含 F1 执行期修订）、裁定 4 setsid 仅测试侧、裁定 5 锚点 session-signals-facade-v1、裁定 6 03d 字面全名禁令（scoped rg 15 文件零命中）、裁定 7 inert 不解除顺序门。(4) SKIPPED：无。(5) 次要 findings 重报：门② 2 条（process-group 留门③已正面回答、R6 措辞不改）、门③ 4 条（trap 摘除留 03d 留意、sizing 无 prototype 执行期已兜底、「无孙进程」措辞备注、mermaid 人工核对）、门④ round1 5 条全修 round2 PASS、门④ round2 范围外观察 2 条备查（R8 nullglob 措辞、$tmp 既有风格）、任务 1.2 round2 F2 已修（tasks.md 两处执行期修订标注）、任务 2.2 次要 1（manifest 措辞按既定实践不改）、任务 2.6 O1/O2 已在完成锚点补齐。(6) 结论：通过，进入合并协议。如果错了代价：验收块缺环会让合并带上未验证据——六块逐项实跑/对照。
- 复盘五问均否：①03c交付与PLAN v5.7/03d设计前提（五模块私有 export 齐备后 aggregator 才发布 public API/marker）逐字一致——本片只新增 signals facade 与其测试、上游七文件零变更、未设置 provider marker，F1 修订全部在测试侧与 spec 文档标注、未触碰 facade 已验收契约，未推翻后续前提；②未暴露计划外工作（F1 属本片信号矩阵规格内缺陷，一轮修复即闭合且经全新 reviewer 独立确认；两处工具用法偏差为机械必要）；③03d已有PLAN v5.7固定口径与03b/03b1/03c验收证据，不变大；④顺序门03b→03b1→03c→03d经任务2.5/2.6机械验证且dependency-present证据（checks=139）已入ledger，不调；⑤无回答不了的新问题（F1 的 bash 异步 subshell SIG_IGN 语义已经 strace 实证并转化为设计修订），不回调研。PLAN v5.7不变，下一片03d-session-remove-prune。
- select：复盘后经 retro→select 建 03d，目录为 specs/2026-09-03-03d-session-remove-prune（new-spec.sh 按创建日取日期前缀，与任务2.5顺序门所核 NEXT=03d-session-remove-prune 仅日期前缀之差；顺序门语义——03d在核对时全部资产缺席——成立且不受影响）。03c 本片收口完毕。
